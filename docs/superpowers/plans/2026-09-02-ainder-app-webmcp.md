# Ainder App WebMCP Implementation Plan

1. Extend WebMCP contract tests for the three browse tools, controller binding, private Profile wording, and mobile avatar geometry.
2. Add a DOM-backed browse controller to `web/assets/browse.js`.
3. Register the read, candidate navigation, and photo navigation tools in `web/assets/webmcp-app.js`.
4. Replace the ambiguous mobile image selector with explicit logo and square avatar classes.
5. Run PHP, JavaScript, syntax, and diff checks.
6. Commit and push `main`, deploy `web/`, then verify the production WebMCP calls and 390px avatar layout.
